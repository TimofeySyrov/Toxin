mixin checkbox-btns({ list=[], }={})
    .checkbox-btns
        each btn in list
            label.check.check-style
                input.check__input(type="checkbox" checked= btn.check)
                span.check__box
                | #{btn.name}